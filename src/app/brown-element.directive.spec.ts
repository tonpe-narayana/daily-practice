import { BrownElementDirective } from './brown-element.directive';

describe('BrownElementDirective', () => {
  it('should create an instance', () => {
    const directive = new BrownElementDirective();
    expect(directive).toBeTruthy();
  });
});
